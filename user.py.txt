# This is how a client would use this project!

from server import Server, ServerConfig

config = ServerConfig(tcl_script, vivado_bash_script, constraints_file, host, port)

server = Server(config)
server.start()


###---Client

from client import Client

my_client = Client(ip, port, username)


my_client.build_bin_file(upload_data_folder, download_folder, only_bin_file)

my_client.build_bin_file(upload_data_folder, download_folder, only_bin_file)

### ---- Server

'''
$ python server.py -file server_config.yaml
'''